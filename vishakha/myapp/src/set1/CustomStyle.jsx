import React from "react";

const CustomStyle = ({backgroundColor,color,borderRadius,padding}) => (

    <button 
    style={{backgroundColor,
        color,
        borderRadius,
        padding,
        border:'none',
        cursor: 'pointer'
    }}>styles</button>

);
export default CustomStyle;